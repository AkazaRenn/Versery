<?xml version="1.0" encoding="utf-8"?>
<Grid
    x:Class="View.Controls.Components.ButtonWithImage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:View">
    <Rectangle
        x:Name="ImageRectangle"
        Loaded="ImageRectangle_Loaded"
        Unloaded="ImageRectangle_Unloaded">
        <Rectangle.Fill>
            <ImageBrush
                x:Name="ImageBrush"
                Stretch="UniformToFill"
                AlignmentX="Center"
                AlignmentY="Center"/>
        </Rectangle.Fill>
    </Rectangle>
    <Button
        x:Name="Button"
        VerticalAlignment="Stretch"
        HorizontalAlignment="Stretch"
        Background="{x:Bind local:Brushes.Transparent}"
        BorderThickness="0"
        CornerRadius="0"
        Padding="0">
        <Button.Resources>
            <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#10FFFFFF"/>
            <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#10000000"/>
        </Button.Resources>
    </Button>
</Grid>
